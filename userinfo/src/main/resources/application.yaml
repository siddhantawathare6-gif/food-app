server:
  port: 9093

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

spring:
  application:
    name: USER-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/userdb
    username: root
    password: system@123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
