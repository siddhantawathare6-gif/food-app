server:
  port: 9097

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

spring:
  application:
    name: ORDER-SERVICE
  mongodb:
    uri: mongodb://localhost:27017/orderdb
  data:
    mongodb:
      repositories:
        type: imperative