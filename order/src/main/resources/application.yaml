server:
  port: ${ORDER_PORT:9097}

#eureka:
#  client:
#    service-url:
#      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka}

spring:
  application:
    name: ${APP_NAME:order-service}
  config:
    import: "optional:configserver:http://localhost:8888"
#  mongodb:
#    uri: ${MONGODB_URI:mongodb://localhost:27017/order_db}
#  data:
#    mongodb:
#      repositories:
#        type: imperative
#    redis:
#      host: ${REDIS_HOST:localhost}
#      port: ${REDIS_PORT:6379}
#    cache:
#      type: redis
#
#management:
#  endpoints:
#    web:
#      exposure:
#        include: health,info,loggers

#resilience4j:
#  retry:
#    instances:
#      userServiceRetry:
#        max-attempts: ${RETRY_MAX_ATTEMPTS:3}
#        wait-duration: ${RETRY_WAIT_DURATION:500ms}
#        retry-exceptions:
#          - org.springframework.web.client.ResourceAccessException
#          - org.springframework.web.client.HttpServerErrorException
#        ignore-exceptions:
#          - org.springframework.web.client.HttpClientErrorException
#logging:
#  level:
#    io.github.resilience4j: DEBUG

#scheduler:
#  order:
#    status:
#      enabled: ${SCHEDULER_ORDER_ENABLED:true}
#      delay: ${SCHEDULER_ORDER_DELAY:30000}